(() => { 'use strict'; const form=document.querySelector('[data-partner-application]'); if(!form)return; const status=document.querySelector('[data-application-status]'); form.addEventListener('submit',async event=>{event.preventDefault();const f=form.elements;status.textContent='Sending…';try{const response=await fetch('/api/partner-applications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({studioName:f.studioName.value,contactName:f.contactName.value,email:f.email.value,phone:f.phone.value,area:f.area.value,address:f.address.value,teamSize:f.teamSize.value,portfolioUrl:f.portfolioUrl.value,instagramUrl:f.instagramUrl.value,services:[...form.querySelectorAll('input[name="services"]:checked')].map(x=>x.value),clientExperience:f.clientExperience.value,selectInterest:f.selectInterest.checked})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Could not send application');form.reset();status.textContent='Received. We will review the fit and come back to you.';}catch(error){status.textContent=error.message;status.classList.add('is-error');}}); })();
